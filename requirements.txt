fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.12
aiosmtplib==3.0.2
python-dotenv==1.0.1
httpx==0.27.2

# Postgres driver (psycopg3 — better Python 3.9 wheel support than psycopg2).
# Used when DATABASE_URL points to Postgres (e.g. Supabase). Harmless on SQLite.
psycopg[binary]>=3.2.4,<4.0

# Amazon S3 storage backend (STORAGE_BACKEND=s3) + storage migration script.
boto3>=1.35,<2.0

# Auth (Phase 2.1+): bcrypt for password hashing, PyJWT for tokens.
bcrypt==4.2.0
PyJWT==2.9.0

# PDF generation (Phase 2.4+): resolution document with embedded signatures.
reportlab==4.2.5
Pillow>=10.4

# Legacy .xls reader — used only by scripts/import_zoho_warranties.py to read
# the historical Zoho invoice exports (openpyxl handles .xlsx, not .xls).
xlrd==2.0.2
