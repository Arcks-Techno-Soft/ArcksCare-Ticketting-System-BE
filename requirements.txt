fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.12
aiosmtplib==3.0.2
python-dotenv==1.0.1
httpx==0.27.2

# Postgres driver — only needed when DATABASE_URL points to Postgres (e.g. Supabase in prod).
# Local dev uses SQLite, so this is commented out to avoid build issues on macOS.
# Uncomment for production:
# psycopg2-binary==2.9.10
